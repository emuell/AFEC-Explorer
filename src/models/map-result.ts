// -------------------------------------------------------------------------------------------------

/* Result for a single entry of an AFEC high level database tSNE map */

export interface MapResult {
  filename: string;
  x: number;
  y: number;  
}